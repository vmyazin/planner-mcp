{
  "buildCommand": "cd dashboard && npm run build",
  "installCommand": "npm install && cd dashboard && npm install && cd ../mcp-server && npm install && npm run build",
  "outputDirectory": "dashboard/.next"
}
